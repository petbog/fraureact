import s from './Slider.module.scss';
import { FC } from 'react';

const Slider: FC = () => {
    return (
        <div className={s.Slider}>
            {/* Your code here */}
        </div>
    );
};

export default Slider;